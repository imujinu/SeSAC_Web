export interface TodoItemInterface {
  id: number;
  text: string;
  done: boolean;
}

export interface Post {
  id: number;
  title: string;
  body: string;
}

export interface MatzipInterface {
  idx?: number;
  imgSrc: string;
  title: string;
  desc: string;
}
